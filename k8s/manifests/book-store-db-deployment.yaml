apiVersion: apps/v1
kind: Deployment
metadata:
  name: book-store-database
  namespace: bookstore
  labels:
    app: book-store-database
spec:
  replicas: 1
  selector:
    matchLabels:
      app: book-store-database
  template:
    metadata:
      labels:
        app: book-store-database
    spec:
      containers:
        - name: book-store-database
          image: postgres:15-alpine
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              valueFrom:
                configMapKeyRef:
                  name: app-config
                  key: DB_DATABASE
            - name: POSTGRES_USER
              valueFrom:
                configMapKeyRef:
                  name: app-config
                  key: DB_USERNAME
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: app-secret
                  key: DB_PASSWORD
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            exec:
              command:
                - /bin/sh
                - -c
                - pg_isready -U postgres
            initialDelaySeconds: 20
            periodSeconds: 10
          readinessProbe:
            exec:
              command:
                - /bin/sh
                - -c
                - pg_isready -U postgres
            initialDelaySeconds: 10
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: book-store-database
  namespace: bookstore
spec:
  selector:
    app: book-store-database
  ports:
    - port: 5432
      targetPort: 5432
  type: ClusterIP